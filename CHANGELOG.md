# Changelog

## 5.7.0 — 2026-09-16

- Rename the public product and Skill ID to ModelRouter / `modelrouter`.
- Add guarded migration from the legacy skill directory and managed AGENTS markers, with backup and ambiguous-duplicate refusal.
- Rename release archives, unpacked roots, state guidance and install examples while retaining historical changelog entries.
- Document current same-name and nearby model-routing projects and distinguish this instruction-first Codex Skill from prompt proxies and model gateways.
- Canonicalize temporary paths in Windows junction tests so GitHub hosted runners treat 8.3 and long path spellings as the same directory.

## 5.6.0 — 2026-09-16

- Add explicit local initialization, atomic bounded outcome recording, read-only summaries and matched route comparisons.
- Keep prompts, credentials and arbitrary payloads out of the event schema; preserve unknown metering as null.
- Include failed, blocked and incomplete work in route cost totals and require complete host token telemetry before a comparison.
- Keep profile updates, model calls, dispatch and savings claims outside the metrics helper.

## 5.5.0 — 2026-09-16

- Shorter discovery and decision-focused skill entry with conditional reference loading.
- Reuse workflow review and limits; continue authorized checks and repairs without adding a routing lifecycle.
- Separate repository AGENTS guidance from portable skill and optional global bridge.
- Document the two user-supplied sources; keep V5.4 portable Python and tests unchanged.
- Select release acceptance notes from VERSION.json.

## 5.4.0 — H13 routing corrections and safer local probe

- Require a reasoning gap for explicit xhigh. Add sourced lifecycle correction limits, stable IDs, escalation and fresh-context requirements.
- Explain unknown cost and model-ID ties. Return structured input errors for policy bootstrap failures and correction mapping mistakes.
- Require a reviewed absolute native executable with expected SHA-256 for the optional probe. Check output roots/ancestors before launch, refuse reparse paths and default to no-clobber JSON publication.
- Add installed payload verification and manifest; document repository-only tests and reject zero-test success. Preserve untrusted evidence and permission boundaries.
- Pin CI action commits and add Windows/Linux Python 3.10/3.12/3.14 test jobs.

## 5.3.0 — Standard Agent Skills distribution

- Moved the portable payload to `skills/model-auto-router/` for standard skill discovery.
- Made `npx skills` and `gh skill install` the primary installation paths.
- Kept the guarded Python installer as an opt-in Codex `AGENTS.md` integration bridge.
- Added `INSTALL.md`, `LICENSE`, `SECURITY.md`, `CONTRIBUTING.md` and a GitHub Actions validation workflow.
- Added repository-level metadata, link, syntax and JSON validation without third-party dependencies.

## 5.2.0 — GitHub-ready distribution

- Added root README with copy-and-run installation instructions.
- Added PowerShell and POSIX shell wrappers with dry-run-by-default behavior.
- Documented project-scoped installation, backups, rollback and the live-validation boundary.
- Kept the Python installer as the single implementation path so wrapper behavior stays consistent.
